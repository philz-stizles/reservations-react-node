export interface ICart {
  products: [];
  cartTotal: number;
  totalQuantity: number;
  noOfProducts: number;
}

export interface ICartProduct {
  products: [];
  cartTotal: number;
  totalQuantity: number;
  noOfProducts: number;
}